# ZuZu — waitlist site

Single-page static site for **ZuZu** — the relationship infrastructure for
independent artists and their fans.

Plain HTML/CSS/JS, no build step. Deployed via GitHub Pages → https://zuzu.music

## Local preview
Serve the folder with any static server, e.g. `python3 -m http.server`, then
open the printed URL.
